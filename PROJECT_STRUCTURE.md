# Dull Winter - 项目结构说明

## 📁 根目录结构

```
F:\dull_winter\
├── .gitignore                  # Git 忽略规则
├── .claude/                    # Claude Code 配置
├── build.gradle                # 根项目构建配置
├── settings.gradle             # Gradle 设置（定义子项目）
├── gradle.properties           # 共享版本和配置
├── gradlew / gradlew.bat       # Gradle 包装器（唯一）
├── gradle/                     # Gradle 配置目录
│
├── README.md                   # 项目说明文档
│
├── dull_radiation/             # 核心模块 ⭐
├── dull_radiation_stuff/       # 物品附加模块
├── dull_radiation_structures/  # 结构生成模块（占位）
└── dull_radiation_variation/   # 变异生物模块（占位）
```

---

## 📦 模块详细结构

### 1. dull_radiation（核心模块）
**路径**: `F:\dull_winter\dull_radiation\`

**功能**：辐射系统核心
- Capability 系统（玩家辐射数据）
- 辐射 API（供其他模块调用）
- 装备防护系统
- 配置管理

**目录结构**：
```
dull_radiation/
├── build.gradle           # 模块构建配置
├── .gitignore            # 模块级 gitignore
├── .gitattributes        # Git 属性配置
├── LICENSE.txt           # 许可证
├── gradle/               # Gradle wrapper（已弃用）
├── src/
│   ├── main/
│   │   ├── java/me/shiming/DullRadiation/
│   │   │   ├── api/              # 公共 API
│   │   │   ├── capability/       # Capability 实现
│   │   │   ├── config/           # 配置类
│   │   │   ├── event/            # 事件处理
│   │   │   ├── Items/            # 核心物品
│   │   │   ├── network/          # 网络包
│   │   │   └── DullRadiation.java # 主类
│   │   └── resources/
│   │       ├── META-INF/         # 模组元数据
│   │       ├── assets/           # 资源文件
│   │       └── pack.mcmeta       # 数据包配置
│   └── test/                    # 测试代码
│
└── [文档文件]
    ├── API_USAGE.md             # API 使用指南
    ├── PROTECTION_CONFIG_GUIDE.md # 防护配置指南
    ├── FINAL_SUMMARY.md         # 开发总结
    └── REWRITE_SUMMARY.md       # 重构总结
```

---

### 2. dull_radiation_stuff（物品附加模块）
**路径**: `F:\dull_winter\dull_radiation_stuff\`

**功能**：物品和方块
- 资源物品（铀、铅）
- 防护装备
- 药品和食品
- 工具和建材
- 11 个方块

**目录结构**：
```
dull_radiation_stuff/
├── build.gradle           # 模块构建配置（依赖核心模块）
├── ASSETS_NEEDED.md       # 资源需求清单
└── src/
    └── main/
        ├── java/me/shiming/DullRadiation/Stuff/
        │   ├── blocks/          # 方块实现
        │   ├── config/          # 配置（空）
        │   ├── items/           # 物品（空）
        │   ├── CreativeTabs.java # 创造标签页
        │   ├── DullRadiationStuff.java # 主类
        │   ├── ModBlocks.java   # 方块注册
        │   └── ModItems.java    # 物品注册
        └── resources/
            ├── META-INF/        # 模组元数据
            └── assets/dull_radiation_stuff/
                ├── lang/        # 语言文件
                ├── blockstates/ # 方块状态
                ├── models/      # 模型文件（待生成）
                └── textures/    # 纹理文件（待制作）
```

---

### 3. dull_radiation_structures（结构生成模块）
**路径**: `F:\dull_winter\dull_radiation_structures\`

**功能**：世界结构生成（占位）
- 铁路系统
- 军事设施
- 核电站废墟

**状态**：框架已搭建，待实现

---

### 4. dull_radiation_variation（变异生物模块）
**路径**: `F:\dull_winter\dull_radiation_variation\`

**功能**：变异生物系统（占位）
- 辐射变异生物
- 新的 AI 行为

**状态**：框架已搭建，待实现

---

## 🗑️ 已清理的文件和目录

### 删除的模块目录
- ✅ `dull_radiation_old/`（旧版本备份，资源已全部复用）

### 删除的构建缓存
- ✅ 所有 `.gradle/` 目录
- ✅ 所有 `build/` 目录
- ✅ 所有 `run/` 和 `run-data/` 目录

### 删除的 IDE 配置
- ✅ 所有 `.idea/` 目录
- ✅ 所有 `.iml` 文件

### 删除的冗余文件
- ✅ `dull_radiation/bin/` 目录（旧编译输出）
- ✅ `dull_radiation/README.txt`（模板文件）
- ✅ `dull_radiation/CREDITS.txt`（模板文件）
- ✅ `dull_radiation/changelog.txt`（模板文件）
- ✅ 子模块中的 `gradlew` 和 `gradlew.bat`（根目录统一管理）

### 删除的清单文件
- ✅ `ASSETS_CHECKLIST.md`（已被 ASSETS_NEEDED.md 替代）
- ✅ `ASSETS_LIST.csv`（已被 ASSETS_NEEDED.md 替代）

---

## 🎯 保留的重要文档

### 根目录
- **README.md** - 项目说明
- **.gitignore** - Git 忽略规则（新增）

### dull_radiation
- **API_USAGE.md** - API 使用文档
- **PROTECTION_CONFIG_GUIDE.md** - 防护配置指南
- **LICENSE.txt** - 许可证

### dull_radiation_stuff
- **ASSETS_NEEDED.md** - 资源文件需求清单（美术必读）

---

## 📊 统计信息

### 代码文件
- **核心模块**：8+ Java 类
- **物品模块**：7+ Java 类（+ 18 物品 + 11 方块）
- **结构模块**：1 个占位类
- **变异模块**：1 个占位类

### 资源需求
- **物品纹理**：18 个 PNG
- **方块纹理**：40 个 PNG
- **模型文件**：48 个 JSON（程序生成）
- **语言文件**：2 个 JSON（中英文）

---

## 🚀 快速开始

### 构建所有模块
```bash
cd F:\dull_winter
gradlew build
```

### 运行游戏（加载所有模块）
```bash
gradlew :dull_radiation:runClient
```

### 清理项目
```bash
gradlew clean
```

---

## 📝 注意事项

1. **不要提交**：
   - `build/`、`.gradle/`、`run/` 等临时目录
   - `.idea/` 等 IDE 配置
   - 个人配置文件

2. **必须提交**：
   - 所有源代码（`src/`）
   - 构建配置（`build.gradle`、`settings.gradle`）
   - Gradle wrapper（`gradlew`、`gradlew.bat`、`gradle/`）
   - 文档文件（`.md`）

3. **统一使用根目录的 Gradle**：
   - 所有子模块不需要自己的 `gradlew`
   - 所有构建命令从根目录执行

---

**最后更新**: 2025-12-26
**版本**: 2.0.0
**维护者**: Shiming
